spring:
  application:
    name: asyncTask
  datasource:
    url: jdbc:mysql://localhost:3306/yqc?useUnicode=true&characterEncoding=utf-8
    username: "root"
    password: "root"
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8081

third-part:
  redis:
    end-point: 127.0.0.1:6379